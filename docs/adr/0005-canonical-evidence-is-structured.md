---
status: accepted
---

# Canonical Evidence is structured

The Kernel and Quality Gate consume schema-validated JSON Evidence Records, while specification-required Markdown artifacts are human-readable Evidence Views generated from those records. Free-form reports and Session transcripts remain useful for inspection but cannot become decision authority, preventing prose parsing or model wording from changing the Gate result.
