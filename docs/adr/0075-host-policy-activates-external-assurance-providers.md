---
status: accepted
---

# Host Policy activates external Assurance Providers

An installed Provider declares capability but cannot add itself to an Assurance Plan. Host Policy selects it as `disabled`, `when-available`, or `required`; once a Provider is selected into a frozen Attempt it is mandatory for that Attempt, and later unavailability produces indeterminate assurance rather than silent omission or substitution.

